import { estourar_erro } from "../src/DarErro";

describe("Dar erro", () => {
  it("deve dar erro ", () => {
    //   const consoleErrrorSpy = jest.spyOn(console, "error");     
    
    //   //Repare que o console.error eh chamado de qualquer jeito, falhando ou passando no  teste
    //   estourar_erro()
    //   expect(consoleErrrorSpy).toHaveBeenCalledTimes(1)
      
    expect(1).toBeTruthy()
    });
});
